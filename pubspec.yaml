name: deffered
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  deferred-components:
    - name: boxComponent
      libraries:
        - package:deffered/d.dart

    - name: boxComponent2
      libraries:
        - package:deffered/first.dart

    - name: boxComponent3
      libraries:
        - package:deffered/second.dart

  uses-material-design: true


